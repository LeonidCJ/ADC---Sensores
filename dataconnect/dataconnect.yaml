specVersion: "v1beta"
serviceId: "SmartCasita"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "SmartCasita-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
